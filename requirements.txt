# SimpleVendas - Requisitos
# Python 3.7+

# Interface Gráfica (já incluído no Python padrão)
# tkinter - já vem com Python

# Banco de dados (já incluído no Python padrão)
# sqlite3 - já vem com Python

# Não são necessárias dependências externas para a versão básica
# O sistema usa apenas bibliotecas padrão do Python

# Para gerar executável (.exe), instale:
# pip install pyinstaller

# Para criar ícone personalizado (opcional):
# pip install Pillow

# Para melhorar a interface (opcional):
# pip install ttkthemes

